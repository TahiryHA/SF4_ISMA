{% extends 'admin/base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block left_sidebar %}
{{ render(controller(
        'App\\Controller\\DefaultController::left',
        { 'active' : 'articles' }
    )) }}
{% endblock %}

{% block page %} 
    <h1>Articles</h1>

    <table class="table">
        <tbody>
           
            <tr>
                <th>Nom</th>
                <td>{{ article.name }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ article.price }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td> <img src="{{ vich_uploader_asset(article) }}" alt=""></td>
            </tr>
            <tr>
                <th>Ajouter le</th>
                <td>{{ article.createdAt ? article.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('articles_index') }}">Retour à la liste</a>

    {{ include('articles/_delete_form.html.twig') }}
{% endblock %}
